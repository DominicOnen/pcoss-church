// Single source of truth for the backend API URL.
// If you ever redeploy the FastAPI backend somewhere else, this is the
// only line you need to change across the whole site.
const API_BASE_URL = "https://pcoss-backend.onrender.com";
